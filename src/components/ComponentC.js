import React from 'react'

const ComponentC = props => {
    return (
        <div>
            <button onClick={props.numbFuncValue}>Get random number</button>
        </div>
    )
}

export default ComponentC;